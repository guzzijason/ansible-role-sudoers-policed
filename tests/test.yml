---
- hosts: localhost
  connection: local
  become: true
  roles:
    - { role: sudoers-policed, team: "example" }
  post_tasks:
     - name: finalize sudoers config
       include_role:
         name: sudoers-policed
         tasks_from: finalize
